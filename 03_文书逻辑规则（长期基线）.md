# 法务AI 3.2 · 文书生成逻辑规则（长期基线）

> 本文件是**文书逻辑的验收基线**，后续每次修改都要先满足这里的原则。最后更新：2026-09-14。

## 一、版式（官方示范文本 1:1）

1. 表格结构照官方模板：标签栏 24.3% / 内容栏 75.7%（官方 40mm / 124.8mm），不因跨列行变成 50%:50%。
2. 字号：标题 22pt、副标题 16pt、**各类大项（当事人信息、诉讼请求、答辩事项、事实与理由、赔偿请求、对纠纷解决方式的意愿…）15pt（小三号）**、正文 10.5pt（五号）、说明区 10.5pt；边框 0.5pt 细黑线，大项无灰底、居中。
3. 落款按官方称谓：起诉状"具状人（签字、盖章）"、答辩状"答辩人"、执行"申请执行人"、国赔"赔偿请求人"；签字与日期上下两行、同左对齐。
4. **不改变官方模板的文字与栏目结构**：只往空格里填内容，不整行顶掉模板（例如模型只回"☑ …"时，保留"无财产□/有财产□"等原栏目）。

## 二、勾选规则（重点）

1. 官方模板有两种勾选写法：`是□`（框在后）与 `□是`（框在前，行政类模板多见）——**两种都要认**。
2. **只有素材能确认时才勾选；确认不了就留空**，由当事人自己勾。
   - 不做"素材没提到就默认否/无"的猜测（担保、保全、仲裁管辖、质量争议、是否涉外、是否协商、是否通知解除等一律不代勾）。
   - 素材明确提到时才勾"是/有"（如：主张诉讼费→是、存在逾期/迟延→是、催促过履行→是、主张担保→是、有财产/有债务→有…）。
3. 选项名与值不一致时按极性对应（素材说"是"而模板是"有□"→勾有；说"无"而模板是"否□"→勾否）。

## 三、逐行填充（映射）规则

按以下顺序取内容，取到即用：

1. **AI 逐行结果**（分析时按"本案由官方要素表行清单"返回，键与行标签一致）；
2. **案由要素关键词**（本金/利息/还款约定/逾期、财产/房产/门面/债务/抚养/赔偿、标的物/价款/交货/验收/催促、违约金/损失…）；
3. **诉讼请求对应项**（传统诉状的请求项通常写得很完整，如财产清单、90 万赔偿）；
4. **通用关键词打分兜底**（覆盖所有案由：行标题关键词 × 要素/请求文本，短标题命中一个专有名词即可，长标题需两个以上）。

补充规则：

- **结构化字段优先**：当事人、合同主体/签订主体、标的总额、请求依据、证据清单由结构化字段填，AI 整段文字不再叠加（避免同一行写两遍）。
- **当事人行判定**：只认"角色开头、无序号、短标签"（原告/被告/答辩人/申请执行人/赔偿请求人…）；正文里提到"被告"的行不算当事人行。
- **金额与日期**：行内"金额："空槽、前置"元"空槽、"尚欠本金/利息　元"、"　年　月　日"空槽，自动填素材里的金额/日期；素材没有就不填。
- 无关项不编造；不确定的写"（待补充）"或留空。

## 四、导出与命名

1. 导出 Word 文件名 = **`<文书名>（<案由>）_<称谓><姓名>_<日期>.doc`**，例如
   `要素式起诉状（民间借贷纠纷）_原告张三_20260914.doc`、
   `民事答辩状（买卖合同纠纷）_答辩人李四_20260914.doc`、
   `强制执行申请书_申请执行人张三_20260914.doc`。
2. Word 文档属性标题（打印标题）与文件名同源：`要素式起诉状（民间借贷纠纷）·原告张三`，**不再只是"法务文书"**。
3. 导出文件带 A4 页面设置、0.5pt 边框、大项 15pt，与页面预览一致。

## 五、缺失项提示

生成前表单显示"官方要素表未填 N 行"，并给出可直接编辑的"官方要素表逐行内容"复核框；改完再生成即按修改后的内容填表。
