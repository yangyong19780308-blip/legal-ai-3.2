# 法务AI 3.2 · 修复说明与发布指引（2026-09-11 · 含两批）

交付文件：**`法务AI3.2_修复版_20260911.html`**（1,388,208 字节，sha256 前 16 位 7C1D4F0D62D98B7E）
线上地址：https://yangyong19780308-blip.github.io/legal-ai-3.2/

---

## 当前状态（2026-09-12 更新）

- 线上 `index.html` **已覆盖为修复版**（1,390,465 字节，blob sha `3db5fdf3…`），线上抓取校验一致；
- 已在线上用真实模型（DeepSeek）跑通起诉状、民事答辩状、强制执行申请书三类；
- 之后又修了一处细节（执行申请书"文书类型"行会把标签括号注释重复带进单元格），当前交付文件为 **1,391,115 字节，sha256 前 16 位 `43FDEABB1A15113A`**；要不要更新到线上由你决定（用同一个上传页覆盖 `index.html` 即可）。

## 财产 / 赔偿行映射补齐（2026-09-12 第二批，用户反馈）

**反馈**：财产部分是空白（案例里有两套房子和一间门面）；经济赔偿 90 万没有映射进诉状。

**原因**：逐行映射的兜底表只认借贷/合同关键词（本金、利率、逾期、担保…），**没有"财产/房产/门面/债务/抚养/赔偿/补偿"这些词**；而且兜底只在"案由要素"里找，不看"诉讼请求"——而传统诉状里的财产清单、90 万请求恰好都写在诉讼请求里。所以只要 AI 没有逐行返回这些行，它们就保持官方空白模板。

**修复**（只改映射层，版式与渲染未动）：

1. 兜底关键词补齐婚姻家庭骨架：财产/房产/房屋/门面/商铺/存款/车辆/股权/分割、债务/欠款、抚养/子女/探望、赔偿/补偿/帮助/损害、婚姻/结婚/感情/分居；
2. 新增"从诉讼请求取对应请求项"的兜底；
3. "金额："空槽自动填入素材里的金额；
4. "无财产□／有财产□""无债务□／有债务□""无此问题□／有此问题□"按内容成对勾选；
5. 当事人角色类选项（原告/被告/其他）不自动勾选，避免"归属"勾错；
6. 模型只给"☑ …"短答案时，保留官方模板原有栏目再补正文，不整行顶掉模板。

**效果**（离婚纠纷：两套房＋一间门面＋90 万）：`2.夫妻共同财产` → 有财产☑ 并列出三处房产（含人民路45号门面房）；`7.离婚损害赔偿` → 离婚损害赔偿☑、金额：900000元；事实与理由里的"夫妻共同财产情况""共同债务情况""赔偿/补偿/经济帮助相关情况"同步填上。样例见 `示例_真实模型输出/离婚纠纷_要素式起诉状_真实模型.doc / .pdf`。

当前交付文件：**1,395,629 字节，sha256 前 16 位 `9E8E6D5081AA3674`**。

## 买卖合同要素行补齐（2026-09-12 第三批，用户反馈）

**反馈**：买卖合同要素诉状里，`2.合同主体`（出卖人/买受人）、`5.合同约定的交货时间、地点、方式、风险承担、安装、调试、验收`、`10.是否催促过履行` 是空白。

**根因**：

1. **当事人行判定过宽**——只要行标题里出现"原告/被告"就被当作当事人信息行（如"15.被告应当支付的利息、违约金、赔偿金"），于是被当成"当事人已填"而**跳过内容映射**；
2. 兜底关键词只有借贷骨架，缺合同主体、标的物、价款、交货/验收/安装/调试/风险承担、催促履行、违约金、损失这些词，也没有"从诉讼请求取值"的兜底。

**修复**（只改映射层）：

- 当事人行改为"角色开头 + 无序号 + 短标签"才认定，正文里提到"被告"不再误判；
- 补齐全套合同类关键词与诉讼请求兜底；合同主体按官方写法填（出卖人（卖方）/买受人（买方）等）；
- 新增"是否催促过履行 / 是否存在迟延履行 / 标的物瑕疵责任 / 质量争议 / 是否协商 / 是否通知解除合同"等默认勾选；
- 给付价款行的前置空槽自动填金额；催促行不再误用交货日期；修正标签回显截断误伤正文前缀。

**效果**（买卖合同纠纷，真实模型）：`2.合同主体` → 出卖人（卖方）：涡阳县惠农商贸有限公司 / 买受人（买方）：张三；`5.合同约定的交货时间…` → 交货时间 2024年2月20日、地点、运输方式、风险承担、安装调试、验收齐全；`10.是否催促过履行` → 是☑ 并写明催促情况；`1.给付价款（元）` → 86000元。样例见 `示例_真实模型输出/买卖合同纠纷_要素式起诉状_真实模型.doc / .pdf`。

当前交付文件：**1,399,236 字节，sha256 前 16 位 `1830EEFE910C1F39`**。

## 全案由通用映射 · 命名 · 小三大项 · 勾选原则（2026-09-14 第四批）

按"同一套逻辑适用所有要素诉状"的要求，本轮做了四处改动（仅映射/命名/字号，模板与版式未动）：

1. **通用关键词兜底**：行标题关键词 × 案由要素/诉讼请求文本打分匹配，覆盖所有案由（医疗费、护理费、物业费、工程款、保险金、停止侵权…不再逐个枚举）；同时支持"框在词前"的勾选写法 `□是/□否`（行政类模板），识别逐字加空格的行标题。
2. **勾选原则改为"确认不了就不勾"**：撤掉全部"没提到就默认否/无"的猜测（担保、保全、仲裁管辖、质量争议、瑕疵责任、协商、通知解除、是否涉外…），只在素材能确认时勾"是/有"；"其他请求"不再自动填"无"。
3. **文书命名带上原告名称与纠纷类型**：导出文件名与 Word 文档属性标题统一为"文书名（案由）_称谓姓名_日期"，例如 `要素式起诉状（民间借贷纠纷）_原告张三_20260914.doc`，不再只是"法务文书"。
4. **大项字号为小三（15pt）**：导出 Word 实测大项 20px＝15pt，标题 22pt、正文 10.5pt、标签栏 24.3% 保持不变。

覆盖检查（53 类起诉状、约 1400 行，"素材确实提到"的前提下仍填不上的行）：787 → 379 → 86 → 42 → **20**（余下基本是"第三人""委托诉讼代理人"和证据附表的"编号/1-1/序号"这类本就该留空的行）。

文书逻辑的长期基线已单独记为 `03_文书逻辑规则（长期基线）.md`，后续改动以该文件为准。

当前交付文件：**1,406,105 字节，sha256 前 16 位 `DDF3EB9B60CD0792`**。

---

## 关于"示例和实际出的文档不一样"（重要）

你反馈的这个现象，原因是：**我先前给的示例是用"模拟 AI 返回"跑出来的引擎测试样张**（为了单独验证版式与逐行映射，不依赖网络和 key），里面填的内容是我预置的完整答案；而产品实际出稿取决于你所用模型返回什么，两者内容自然不同。

现在已经用**真实模型（DeepSeek `deepseek-chat` + 真实 API）**重跑了三类文书，并修掉了真实输出才暴露出来的两个问题：

1. **当事人行被写两遍**：真实模型会在 `elements` 里再返回一遍当事人（如"姓名：张三，性别：男…身份证号…"），旧逻辑把它接到已填好的当事人单元格后面。现在改为**结构化字段优先**：当事人、签订主体、标的总额、请求依据、证据清单这几行由结构化数据负责，不再叠加 AI 整段文字。
2. **执行申请书的选项没打勾**：模型返回"民事判决书"时，模板里的"判决书□"没有勾上。现在新增**按行内选项名自动打勾**（"本金□→本金☑""判决书□→判决书☑"），并避免了"判决书"误命中"刑事附带民事判决书"这类后缀串位。

真实模型的成稿放在 **`示例_真实模型输出/`**（含 PDF 预览）；先前那批模拟样张移到 `示例_修复前后对照/` 并标注"模拟数据"，仅用于看版式差异，不代表产品成稿内容。

> 另外提醒：如果是在**线上页面**跑的，请先确认线上是否已经覆盖成修复版——没覆盖的话，线上仍是 09-06 的旧版（近似表格、缺项多），当然和示例不一样。

---

## 批次一：你反馈的三个问题（已修复）

### 1. 原被告信息没有映射出

原因有三：① 官方模板里一部分案由（离婚、劳动争议、部分行政/刑事自诉）的当事人行**不标注「（自然人）/（法人）」**，老逻辑只认带标注的行；② 单位当事人**没有法定代表人、统一社会信用代码、注册地、企业类型、所有制性质**这些字段，AI 提取到了也无处安放；③ 单位识别只看"公司/厂/店"结尾，遇到「XX商行、XX经营部、XX服务部、XX合作社」会误判成自然人。

修复：当事人行不再依赖标注（按实际身份填）；表单补齐单位字段与经常居住地、工作单位、职务；身份判断改为「AI 给的类型 → 名称关键词 → 兜底」三级；单位当事人自动勾选「有限责任公司☑」「民营☑」等选项。

### 2. 文档中的表格布局与模板不一样

原因：官方是标签栏 40mm∶内容栏 124.8mm（24.3%∶75.7%），但表格第一行是整行跨列的"填写说明"，浏览器固定表格布局据此**把两列平均成 50%∶50%**；大项还多了官方没有的灰底。

修复：加 `colgroup` 固定 24.3%∶75.7%；标题 22pt、副标题 16pt、大项 15pt、正文 10.5pt 仿宋、边框 0.5pt、去灰底；导出 Word 增加 A4 页面设置。

### 3. 缺失很多遗漏项目

原因：老逻辑只把要素整段塞进"可完整表述…"两行，官方要素表的 1.本金、2.利息、借款期限、利率、还款方式、逾期、请求依据、证据清单等**逐行小项全部留空**（46 行往往只有 6 行有内容）。

修复：三级映射逐行填表 —— ① AI 按官方行清单逐行返回内容 → ② 案由要素按行关键词兜底 → ③ 官方空白模板兜底（不编造）；自动打勾、回填金额/日期空槽；新增「官方要素表逐行内容」复核框可手改；生成前提示未填行数。实测同一案例 **6 行 → 30 行**有内容。

---

## 批次二：答辩状 / 强制执行 / 国家赔偿接入官方版式（本次新增）

原来只有起诉状 53 类走"一比一"官方版式，答辩状、执行申请书、国家赔偿申请书仍是旧近似表格。本批已全部补齐：

| 文书类型 | 新增套数 | 官方版式来源 |
| --- | --- | --- |
| 民事答辩状 | 33 | 法〔2025〕82号 PDF（按案由逐页提取） |
| 行政答辩状 | 2（含行政答辩状（通用）） | 同上 |
| 刑事（附带民事）自诉答辩状 | 4 | 同上 |
| 国家赔偿答辩状 | 4 | 同上 |
| 执行 / 申请类申请书（强制执行、参与分配、执行异议、执行复议、执行担保、执行监督、不予执行仲裁裁决、暂时解除乘机乘车限制、确认优先购买权） | 9 | 同上 |
| 国家赔偿申请书 | 4 | 同上 |

**官方版式总数：54 套 → 110 套。**

配套改动：
- 按案由自动选择正确的官方文本（同一"答辩状"分民事/行政/刑事自诉/国家赔偿四类，自动匹配）；
- 表头「案号 / 案由」行自动填案由、案号留空由法院或当事人填写；
- 落款按官方称谓：起诉状「具状人（签字、盖章）」、答辩状「答辩人（签字、盖章）」、执行「申请执行人（签字、盖章）」、国赔「赔偿请求人（签字、盖章）」；
- 答辩/执行/国赔的 AI 提示同样带上"本案由官方要素表行清单"，逐行回填（含 ☑ 勾选）；
- 修正案由分类：原来「刑事改判无罪赔偿、违法刑事拘留赔偿」被误放进"刑事自诉状"分组，现归入"国家赔偿申请书"，该分组由 2 项修正为 4 项。

---

## 发布方式（二选一）

线上页面 = 仓库 `yangyong19780308-blip/legal-ai-3.2` 根目录 `index.html`，必须覆盖它才会生效。

**方式一：网页手动覆盖（约 1 分钟，无需 token）**

1. 打开 https://github.com/yangyong19780308-blip/legal-ai-3.2
2. Add file → Upload files，把 `法务AI3.2_修复版_20260911.html` 拖进去
3. 文件名改成 `index.html`（覆盖同名文件）→ Commit changes
4. 等 1–2 分钟刷新线上页面

**方式二：脚本自动覆盖（需要 GitHub token）**

```powershell
setx GITHUB_TOKEN "你的token"      # Fine-grained token：仓库 legal-ai-3.2，权限 Contents: Read and write
cd <本 outputs 目录>
python 发布脚本_publish.py --file 法务AI3.2_修复版_20260911.html --dry-run
python 发布脚本_publish.py --file 法务AI3.2_修复版_20260911.html
```

脚本会先校验文件特征（含新版标记），再覆盖远端 `index.html`，并轮询线上页面直到检测到新版生效。

> 本机当前**没有配置 GITHUB_TOKEN**，所以自动发布这一步需要你先设好 token；按项目规矩，未确认不上传。你说一声、并把 token 设好，我可以直接执行。

---

## 还没做的部分

1. **第三人意见陈述书（3 类）**：工具目前没有该文书入口（官方 PDF 里有版式，后续可加）。
2. 官方 PDF 中"实例"页（示范填写）未做对照展示。
3. 多原告/多被告（同一方多个当事人）仍是一套字段，官方模板允许复制粘贴行，后续可扩展。
4. 调解意愿类选项保持空白（不替当事人表态）。
